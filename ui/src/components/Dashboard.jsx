import React from 'react';
import { connect } from 'react-redux';

const Dashboard = props => <div className="container">HI</div>;

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(Dashboard);
